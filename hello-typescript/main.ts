var myVar = 'my variable';

function myFunc(x, y) {
    return x + y;
}

//ES6 or ES 2015
let num = 2;
const PI = 3.14;

var numbers = [1, 2, 3];
numbers.map(function(value) {
    return value * 2;
});

numbers.map(value => value * 2);

class MyMath {
    mySum(x, y) {
        return x + y;
    }
}

var n1: any = 'String';
n1 = 4;